#!/usr/bin/env sh
set -eu

DEVTOOLS_DIR="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
PROJECT_DIR="${PROJECT_DIR:-$PWD}"

PROJECT_DIR="$PROJECT_DIR" \
  docker compose -f "$DEVTOOLS_DIR/docker-compose.yml" run --rm --build gh-cli "$@"
